@model Shop.Web.ViewModels.ShopCartViewModel
<div class="container">
    @foreach (var el in Model.ShopCart.ListShopItems)
    {
        <div class="alert alert-warning mt-3">
            <b>Automobile:</b> @el.Car.Name<br />
            <b>Price:</b> @el.Car.Price.ToString("c")
        </div>
    }
    <hr />
    <a class="btn btn-danger" asp-controller="Order" asp-action="CheckOut">Purchase</a>
</div>